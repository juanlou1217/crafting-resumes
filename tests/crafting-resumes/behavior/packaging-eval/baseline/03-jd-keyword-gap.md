状态：`draft_under_review`（私下审阅稿）

建议将已确认内容写为：

**核心技能：** `React（页面开发）`

并把“React 页面开发”放在最相关经历的靠前位置，但不补写“负责、主导、接口联调、性能优化、上线”等未经确认的内容。

Kubernetes 是 JD 的高权重必备项，但当前属于真实缺口，处理为 `do_not_force`：简历中不要加入 `Kubernetes`、`K8s`、`容器编排`、`容器化部署` 或 `云原生` 等无证据关键词。React 页面开发也不能作为 Kubernetes 的替代证据。

这项硬门仍可能导致 ATS 或 HR 筛除；关键词优化只能强化 React 证据，不能消除 Kubernetes 缺口。后续取得可验证的学习或个人项目证据后，再按真实场景标注，不能写成工作经验或“熟练掌握”。
